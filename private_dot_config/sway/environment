# Sway Environment Variables
# This file is sourced by Fedora Sway on startup
# Located at: ~/.config/sway/environment

# Electron apps (Obsidian, VSCode, etc.) use native Wayland
ELECTRON_OZONE_PLATFORM_HINT=wayland

# Screen sharing support
XDG_CURRENT_DESKTOP=sway
XDG_SESSION_TYPE=wayland
XDG_SESSION_DESKTOP=sway
SDL_VIDEODRIVER=wayland
_JAVA_AWT_WM_NONREPARENTING=1
QT_QPA_PLATFORM=wayland

# Firefox and other apps
MOZ_ENABLE_WAYLAND=1

# Optional: Enable wayland for Chromium/Electron
# CHROMIUM_FLAGS="--ozone-platform=wayland"
